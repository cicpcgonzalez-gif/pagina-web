services:
  - type: web
    name: pagina-web
    env: node
    branch: main
    rootDir: src/screens/web
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NEXT_PUBLIC_API_URL
        value: https://megarifas-backend.onrender.com
      - key: NODE_VERSION
        value: "20"